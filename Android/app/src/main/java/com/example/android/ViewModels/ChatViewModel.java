package com.example.android.ViewModels;

import androidx.lifecycle.ViewModel;

public class ChatViewModel  extends ViewModel {

/*         private MsgsRepository repository; //handles all interaction with db and api

         private LiveData<List<Message>> msgs;

        public ChatViewModel(MsgsRepository repository, LiveData<List<Message>> msgs) {
                this.repository = new MsgsRepository();
                this.msgs = repository.getAll();
        }

        public LiveData<List<Message>> get() { return msgs; }

         public void add(Message msg) { repository.add(msg); }

         public void delete(Message msg) { repository.delete(pomsgst); }

         public void reload() { repository.reload(); }*/
 }
